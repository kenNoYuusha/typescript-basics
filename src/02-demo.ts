/*
    Transpilar ts a js con archivo de configuracion

    --inicializar el archivo de config 
    npx tsc --init

    --editar archivo de configuracion
    cambiar campos por ejemplo outDir, rootDir

    --una vez editado el archivo de configuracion correr el siguiete comando, leera el archivo de configuracion y en base a ello realizara la transpilacion correspondiente.

    npx tsc

    --para mantener escuchando cambios en los archivos ts

    npx tsc --wathch o npx tsc -w
*/

const numbers = [1, 3, 4, 5, 6, 7];
